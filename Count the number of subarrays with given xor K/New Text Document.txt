int Solution::solve(vector<int> &A, int B) {
    unordered_map<int,int>mp;
    int x=0;
    int ans=0;
    mp[0]++;
    for(int i=0;i<A.size();i++)
    {
        x = x^A[i];
        
        ans= ans+mp[x^B];
        mp[x]++;
    }
    return ans;
    
}